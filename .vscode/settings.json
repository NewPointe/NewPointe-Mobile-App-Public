{
    "typescript.check.workspaceVersion": false,
    "files.exclude": {
        "**/*.js": { "when": "$(basename).ts" },
        "**/*.map": { "when": "$(basename).map" }
    },
    "search.exclude": {
        "**/node_modules": true,
        "platforms/": true,
        "lib/": true
    }
}